# Playwright - 브라우저 자동화
playwright==1.40.0

# Pytest - 테스트 프레임워크
pytest==7.4.3

# Python-dotenv - 환경변수 관리
python-dotenv==1.0.0

# OpenCV - 이미지 처리 (캡차 OCR)
opencv-python==4.8.1.78

# EasyOCR - 캡차 인식
easyocr==1.7.0

# Pillow - 이미지 처리
Pillow==10.1.0

# PyTesseract - OCR (필요시)
pytesseract==0.3.10

# Requests - HTTP 요청 (API 테스트용, 동기식)
requests==2.31.0

# AIOHTTP - 비동기 HTTP 요청 (필요시)
aiohttp==3.9.1

# NumPy - 수치 계산 (OpenCV 의존성)
numpy==1.26.2
