name: Daily Playwright Test

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 0 * * *'

jobs:
  test:
    runs-on: [self-hosted]
    env:
      PYTHON_PATH: C:\Users\NHN\AppData\Local\Programs\Python\Python311
      PYTHONIOENCODING: utf-8
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Install dependencies
        shell: cmd
        run: |
          "${{ env.PYTHON_PATH }}\python.exe" -m pip install --user -r requirements.txt
          "${{ env.PYTHON_PATH }}\python.exe" -m playwright install
      
      - name: Create .env file
        shell: cmd
        run: |
          (
            echo fr_username=${{ secrets.FR_USERNAME }}
            echo fr_password=${{ secrets.FR_PASSWORD }}
            echo va_username=${{ secrets.VA_USERNAME }}
            echo va_password=${{ secrets.VA_PASSWORD }}
            echo wa1_username=${{ secrets.WA1_USERNAME }}
            echo wa1_password=${{ secrets.WA1_PASSWORD }}
            echo wa2_username=${{ secrets.WA2_USERNAME }}
            echo wa2_password=${{ secrets.WA2_PASSWORD }}
            echo mo_username=${{ secrets.MO_USERNAME }}
            echo mo_password=${{ secrets.MO_PASSWORD }}
          ) > .env
      
      - name: Run precondition tests
        shell: cmd
        run: |
          chcp 65001
          "${{ env.PYTHON_PATH }}\python.exe" -m pytest tests/precondition -v -s --tb=short

      - name: Run main Playwright tests
        shell: cmd
        run: |
          chcp 65001
          "${{ env.PYTHON_PATH }}\python.exe" -m pytest tests/ --ignore=tests/precondition -v -s --tb=short
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: output/
          retention-days: 7
      
      - name: Notify on failure
        if: failure()
        shell: cmd
        run: echo Test failed! Check output/ folder for screenshots.

      - name: Send email with results
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.dooray.com        # 사내 SMTP 주소로 변경
          server_port: 465                            # 포트(보통 587 또는 25, SSL은 465)
          username: ${{ secrets.MAIL_USER }}          # 사내 메일 계정 (Secrets에 등록)
          password: ${{ secrets.MAIL_PASS }}          # 사내 메일 비밀번호 (Secrets에 등록)
          subject: FG Automation Playwright Test Results
          to: taehyung-moon@nhnservice.com, seungbin.an@nhnservice.com                         # 받을 사람 메일 주소
          from: ${{ secrets.MAIL_USER }}              # 보낼 사람 메일 주소
          body: |
            FG Automation Playwright 테스트가 완료되었습니다.
            📎 결과 파일은 GitHub Actions Artifacts에서 확인하세요.

            실행 일시: ${{ github.run_started_at }}
            워크플로 링크: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          attachments: output/